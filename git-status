#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="${DIR}/gitradar-engine"
if [[ -x "${BIN}" ]]; then
    exec "${BIN}" --status
else
    echo '{"text":"GIT: OFFLINE","tooltip":"Engine binary not found","class":"alert"}'
fi
